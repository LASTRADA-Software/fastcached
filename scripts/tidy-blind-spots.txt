# Translation units that contribute NOTHING on the platform a given clang-tidy leg
# runs on, and which OTHER leg (if any) does read them.
#
# **"Blind" is a property of (unit, set of legs), not of a unit.** A unit inside
# `#if defined(_WIN32)` is empty on Linux and real on Windows, so whether anything
# analyses it depends on which legs exist -- and the answer changes when a leg is
# added. The middle column is therefore a LIST of legs, and `none` means exactly
# that: no analyser this project runs reads this unit.
#
# `scripts/check-tidy-blind-spots.sh` MEASURES the blind set for the build it is
# given and refuses when this table disagrees, in either direction. It also refuses
# a row naming a leg that does not exist in `.github/workflows/build.yml`, so a leg
# that is deleted or renamed cannot leave rows quietly claiming coverage. The list
# is never the evidence -- the measurement is.
#
# The count of units reached by NO leg is printed on every run. That number is
# https://github.com/LASTRADA-Software/fastcached/issues/858, and it is 5 until a
# macOS leg exists.
#
# Format: <path><TAB><reached-by, comma-separated, or `none`><TAB><reason>
src/FastCache/Async/IocpReactor.cpp	clang-tidy-windows	`#if defined(_WIN32)`
src/FastCache/Async/IocpReactor_test.cpp	clang-tidy-windows	`#if defined(_WIN32)`
src/FastCache/Net/IocpConnector.cpp	clang-tidy-windows	`#if defined(_WIN32)`
src/FastCache/Net/IocpConnector_test.cpp	clang-tidy-windows	`#if defined(_WIN32)`
src/FastCache/Net/IocpSocket.cpp	clang-tidy-windows	`#if defined(_WIN32)`
src/FastCache/Net/IocpSocket_test.cpp	clang-tidy-windows	`#if defined(_WIN32)`
src/FastCache/Async/KqueueReactor.cpp	none	`#if defined(__APPLE__)`; no macOS analyser leg exists
src/FastCache/Async/KqueueReactor_test.cpp	none	`#if defined(__APPLE__)`; no macOS analyser leg exists
src/FastCache/Net/KqueueConnector.cpp	none	`#if defined(__APPLE__)`; no macOS analyser leg exists
src/FastCache/Net/KqueueSocket.cpp	none	`#if defined(__APPLE__)`; no macOS analyser leg exists
src/FastCache/Net/KqueueSocket_test.cpp	none	`#if defined(__APPLE__)`; no macOS analyser leg exists
